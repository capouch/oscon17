/* Asset.js: Render a single image and display its associated data
  Brian Capouch begun 10 December 2016
*/

import React from 'react'
import { Section } from 'neal-react'

// For "goto" purposes
import { browserHistory } from 'react-router/es'

// This component will always have a parameter: a record _id

// Step one: Fetch and format the imageRecs

// Step two: Make the image clickable

// Step three: Render page
